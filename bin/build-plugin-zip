#!/usr/bin/env ruby
# frozen_string_literal: true

# Alias for package-wordpress-zip: compile assets, assert SDK, write installable ZIP.
require_relative "package_boundaries"

skip_compile = ARGV.include?("--skip-compile")
path = PackageBoundaries.build_wordpress_zip!(compile: !skip_compile)
puts path
