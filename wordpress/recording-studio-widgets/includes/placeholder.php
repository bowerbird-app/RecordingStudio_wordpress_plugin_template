<?php
/**
 * Shared placeholder copy for the dynamic block.
 *
 * @package RecordingStudio
 */

if ( ! function_exists( 'recording_studio_widget_placeholder_text' ) ) {
	/**
	 * Returns the safe placeholder text shown in the editor and on the published page.
	 *
	 * @return string
	 */
	function recording_studio_widget_placeholder_text() {
		return 'RecordingStudio Widget placeholder. This block does not load a live widget yet.';
	}
}
