<% page_title = @page_recording.recordable&.title.presence || "Page" %>
<% dummy_page_nav(title: page_title) %>

<iframe
  title="<%= page_title %>"
  src="<%= embed_preview_page_path(@page_recording) %>"
  class="w-full min-h-[24rem] border-0"
></iframe>
